import WebsiteShell from "@/components/website/WebsiteShell"; import PackageCard from "@/components/website/PackageCard"; import SectionTitle from "@/components/website/SectionTitle"; import { getPackages } from "@/lib/packages";
export const metadata={title:"Travel Packages",description:"Browse Viago domestic, international, honeymoon, family and adventure holiday packages."}; export const dynamic="force-dynamic";
export default async function PackagesPage({searchParams}){const q=await searchParams; const destination=q.destination||""; const category=q.category||""; let items=await getPackages({destination,category:category||undefined}); const maxPrice=Number(q.maxPrice||0); const maxDays=Number(q.maxDays||0); if(maxPrice) items=items.filter(p=>Number(p.price)<=maxPrice); if(maxDays) items=items.filter(p=>p.days<=maxDays); return <WebsiteShell><section className="bg-[#f7faff] py-16"><div className="container-site"><SectionTitle eyebrow="Travel packages" title="Find a trip that fits your mood." text="Use the filters to narrow things down, then open any package for the full itinerary and inclusions."/></div></section><section className="section-pad"><div className="container-site grid gap-8 lg:grid-cols-[280px_1fr]"><form className="h-fit rounded-3xl border border-slate-200 p-6 lg:sticky lg:top-24"><h2 className="text-lg font-black">Filter packages</h2><label className="mt-5 block text-sm font-bold">Destination<input name="destination" defaultValue={destination} placeholder="e.g. Kerala" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-[#3A86FF]"/></label><label className="mt-4 block text-sm font-bold">Category<select name="category" defaultValue={category} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"><option value="">All categories</option>{['Domestic','International','Honeymoon','Family','Adventure','Weekend Trips'].map(x=><option key={x}>{x}</option>)}</select></label><label className="mt-4 block text-sm font-bold">Max price<select name="maxPrice" defaultValue={q.maxPrice||""} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"><option value="">Any</option><option value="20000">₹20,000</option><option value="30000">₹30,000</option><option value="50000">₹50,000</option></select></label><label className="mt-4 block text-sm font-bold">Max duration<select name="maxDays" defaultValue={q.maxDays||""} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"><option value="">Any</option><option value="3">3 days</option><option value="5">5 days</option><option value="7">7 days</option></select></label><button className="mt-6 w-full rounded-xl bg-[#3A86FF] px-4 py-3 font-bold text-white">Apply filters</button></form><div><div className="mb-6 text-sm text-slate-500">{items.length} package{items.length!==1?'s':''} found</div><div className="grid gap-7 md:grid-cols-2 xl:grid-cols-3">{items.map(p=><PackageCard key={p.id} pkg={p}/>)}</div>{!items.length&&<div className="rounded-3xl bg-slate-50 p-10 text-center text-slate-500">No packages match those filters.</div>}</div></div></section></WebsiteShell>}
