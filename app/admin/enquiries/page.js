import prisma from "@/lib/prisma"; export const dynamic='force-dynamic'; export default async function Enquiries(){let items=[];try{items=await prisma.enquiry.findMany({include:{package:true},orderBy:{createdAt:'desc'},take:100})}catch{}return <><p className="text-sm text-slate-400">Customer requests</p><h1 className="text-3xl font-black">Enquiries</h1><div className="mt-8 grid gap-4">{items.map(e=><div key={e.id} className="rounded-3xl bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="font-black">{e.name}</h2><p className="mt-1 text-sm text-slate-500">{e.phone}{e.email?` · ${e.email}`:''}</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-[#3A86FF]">{e.status}</span></div><p className="mt-4 text-sm leading-6 text-slate-600">{e.message}</p><div className="mt-3 text-xs text-slate-400">{e.package?.title||e.destination||'General enquiry'} · {new Date(e.createdAt).toLocaleString()}</div></div>)}{!items.length&&<div className="rounded-3xl bg-white p-10 text-center text-slate-400">No enquiries yet.</div>}</div></>}
